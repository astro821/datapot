# Copy to docker/.env (gitignored) or export before compose.
# Compose does not start MongoDB. Point this at a separate server.
#
# MongoDB on the Docker host:
# DPOT_DB_URL=mongodb://host.docker.internal:27017/datapot
#
# A remote MongoDB server:
# DPOT_DB_URL=mongodb://mongo.example:27017/datapot
